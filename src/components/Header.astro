---
const nav = [
  { href: '#about',      label: 'About'      },
  { href: '#shipped',    label: 'Shipped'    },
  { href: '#experience', label: 'Experience' },
  { href: '#skills',     label: 'Skills'     },
  { href: '#personal',   label: 'Personal'   },
  { href: '#contact',    label: 'Contact'    },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-[#FAF7F2]/95 backdrop-blur border-b border-teal/20">
  <div class="max-w-4xl mx-auto px-6 h-14 flex items-center justify-between">
    <a
      href="#about"
      class="font-semibold text-surface hover:text-brand transition-colors text-sm whitespace-nowrap"
    >
      Pete C<span
        id="name-reveal"
        class="inline-block"
        style="clip-path: inset(0 100% 0 0)"
      >hitvicheankul</span>
    </a>
    <nav>
      <ul class="flex items-center gap-1">
        {nav.map(({ href, label }) => (
          <li>
            <a
              href={href}
              class="px-3 py-1.5 text-sm text-mid hover:text-brand hover:bg-brand/10 rounded-md transition-colors"
            >
              {label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<script>
  const el = document.getElementById('name-reveal') as HTMLElement;

  function progress(): number {
    const max = document.documentElement.scrollHeight - window.innerHeight;
    return max > 0 ? Math.min(1, Math.max(0, window.scrollY / max)) : 0;
  }

  function apply() {
    // Reveal left-to-right: reduce the right clip value from 100% → 0%
    el.style.clipPath = `inset(0 ${(1 - progress()) * 100}% 0 0)`;
  }

  apply();
  window.addEventListener('scroll', apply, { passive: true });
</script>
